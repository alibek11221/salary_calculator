-- Template for the initial salary_changes seed.
-- Copy this file to `20251207151005_insert_salary_changes.sql` (drop the .example
-- suffix) and replace the placeholder values with your own before running
-- `make run-migrations`. The real file is git-ignored so personal salary data
-- never lands in version control.
--
-- +goose Up
-- +goose StatementBegin
INSERT INTO salary_changes (salary, change_from)
VALUES (100000, '2024_04'),
       (120000, '2025_03'),
       (150000, '2025_09');
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DELETE
FROM salary_changes
where change_from IN ('2024_04', '2025_03', '2025_09')
-- +goose StatementEnd
